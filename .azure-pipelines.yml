trigger:
- main

pool:
  vmImage: ubuntu-latest

stages:
- stage: AzureLogin
  jobs:
  - job: login
    steps:
    - task: AzureCLI@2
      inputs:
        azureSubscription: 'AzureDevopsSP-MSDN'
        scriptType: bash
        scriptLocation: inlineScript
        inlineScript: |
          echo "✅ Logged in to Azure!"

- stage: TerraformInfra
  dependsOn: AzureLogin
  variables:
  - group: terraform-auth

  jobs:
  - job: buildInfra
    steps:
    - task: AzureCLI@2
      inputs:
        azureSubscription: 'AzureDevopsSP-MSDN'
        scriptType: bash
        scriptLocation: inlineScript
        inlineScript: |
          echo "🔐 Registering AKS provider if needed..."
          az provider register --namespace Microsoft.ContainerService
          while true; do
            STATUS=$(az provider show --namespace Microsoft.ContainerService --query registrationState -o tsv)
            echo "Registration status: $STATUS"
            if [ "$STATUS" = "Registered" ]; then
              break
            fi
            echo "Waiting for provider to register..."
            sleep 5
          done

    - script: |
        echo "🧱 Installing Terraform..."
        curl -sLo terraform.zip https://releases.hashicorp.com/terraform/1.5.0/terraform_1.5.0_linux_amd64.zip
        unzip terraform.zip
        sudo mv terraform /usr/local/bin/
        terraform -v
      displayName: 'Install Terraform'

    - script: |
        cd infra

        echo "🗂️ Initializing Terraform..."
        terraform init -upgrade

        echo "🔍 Checking if resource group exists in Azure..."
        az group exists --name toyshop-rg | grep true || SKIP_IMPORT=true

        if [ "$SKIP_IMPORT" != "true" ]; then
          echo "📦 Importing existing RG into Terraform state (if not already)..."
          terraform state list | grep azurerm_resource_group.rg || \
          terraform import azurerm_resource_group.rg "/subscriptions/$(ARM_SUBSCRIPTION_ID)/resourceGroups/toyshop-rg"
        else
          echo "✅ RG does not exist — skipping import."
        fi

        echo "🚀 Applying Terraform"
        terraform apply -auto-approve
      displayName: 'Run Terraform'
      env:
        TF_VAR_client_id: $(ARM_CLIENT_ID)
        TF_VAR_client_secret: $(ARM_CLIENT_SECRET)
        TF_VAR_subscription_id: $(ARM_SUBSCRIPTION_ID)
        TF_VAR_tenant_id: $(ARM_TENANT_ID)

- stage: BuildImages
  dependsOn: TerraformInfra
  jobs:
  - job: buildpushImages
    steps:
    - task: AzureCLI@2
      inputs:
        azureSubscription: 'AzureDevopsSP-MSDN'
        scriptType: bash
        scriptLocation: inlineScript
        inlineScript: |
          echo "✅ Logged in to Azure for ACR"

    - script: |
        make get-azure-resources
      displayName: 'Builds and pushes Docker images to ACR'

- stage: DeployToAKS
  dependsOn: BuildImages
  jobs:
  - job: Deploy
    steps:
    - task: AzureCLI@2
      inputs:
        azureSubscription: 'AzureDevopsSP-MSDN'
        scriptType: bash
        scriptLocation: inlineScript
        inlineScript: |
          echo "✅ Logged in to Azure for Helm deployment"

    - script: |
        make deploy-azure
      displayName: 'Deploy to AKS cluster'

- stage: InstallFlux
  dependsOn: DeployToAKS
  jobs:
  - job: fluxInstall
    steps:
    - checkout: self

    - task: AzureCLI@2
      inputs:
        azureSubscription: 'AzureDevopsSP-MSDN'
        scriptType: bash
        scriptLocation: inlineScript
        inlineScript: |
          echo "🔑 Getting AKS credentials"
          az aks get-credentials --resource-group toyshop-rg --name toyshop-aks

          echo "🔧 Installing Flux CLI"
          curl -s https://fluxcd.io/install.sh | bash
          export PATH=$PATH:/home/vsts/.flux/bin

          echo "🚀 Bootstrapping Flux"
          flux bootstrap github \
            --owner=ukez15 \
            --repository=technical-code-challenge \
            --branch=main \
            --path=clusters/prod \
            --personal
